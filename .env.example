# TruthGuard — Root-level environment variables
# Copy to .env (git-ignored) and fill in values.
# Most secrets live in apps/api/.env — see apps/api/.env.example

# MongoDB local dev credentials (used by Docker Compose)
MONGO_ROOT_USER=root
MONGO_ROOT_PASSWORD=devpassword
MONGO_DB_NAME=truthguard
