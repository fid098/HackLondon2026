# TruthGuard API — Environment Variables
# ─────────────────────────────────────────────────────────────────────────────
# Copy this file to .env and fill in your values.
# .env is git-ignored and must NEVER be committed.
# This example file IS committed and serves as documentation.
# ─────────────────────────────────────────────────────────────────────────────

# ─── Core ────────────────────────────────────────────────────────────────────
ENVIRONMENT=development
DEBUG=false

# ─── MongoDB ─────────────────────────────────────────────────────────────────
# Local dev: connects to the Docker Compose mongo service (see docker-compose.yml)
MONGO_URI=mongodb://root:devpassword@localhost:27017/truthguard?authSource=admin
# MongoDB Atlas (production):
# MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/truthguard?retryWrites=true&w=majority
MONGO_DB_NAME=truthguard

# ─── CORS ────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins.
# Add chrome-extension://<extension-id> once the extension is published.
CORS_ORIGINS_STR=http://localhost:5173,http://localhost:3000

# ─── AI — Google Gemini ───────────────────────────────────────────────────────
# Get your key at: https://aistudio.google.com/
GEMINI_API_KEY=

# Set to false to use real Gemini API (requires GEMINI_API_KEY above)
# Leave true for local dev / tests — mock responses are returned instead
AI_MOCK_MODE=true

# ─── Optional integrations ───────────────────────────────────────────────────
# These degrade gracefully if not set (empty results, logged warning).

# Serper.dev — Google Search API for AI debate agent research
# Get key at: https://serper.dev/
SERPER_API_KEY=

# Google Fact Check Tools API — cross-reference against verified fact-checks
# Enable at: https://developers.google.com/fact-check/tools/api
GOOGLE_FACT_CHECK_API_KEY=

# ─── Auth (Phase 1) ───────────────────────────────────────────────────────────
# IMPORTANT: Generate a strong random secret for production!
# python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=changeme-in-production-use-a-long-random-string
JWT_ALGORITHM=HS256
JWT_EXPIRY_HOURS=24
